# snap-call-center-access static site

This folder is a GitHub Pages-ready static site for the paper landing page.

## Pages

- `index.html`: landing page
- `policy.html`: print-friendly policy one-pager

## Publish on GitHub Pages

Create a GitHub repository named `snap-call-center-access`. In the repository settings, enable Pages from the `docs/` folder on the default branch.
